'use client'

import React from 'react'

export const ClientBetterAuthProviders: React.FC<React.PropsWithChildren> = ({ children }) => {
    return <>{children}</>
}

export default ClientBetterAuthProviders